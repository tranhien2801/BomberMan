package uet.oop.bomberman.GameControl;

import uet.oop.bomberman.Level.Map;

public class canMove {
    /*public boolean checkMove;

    public boolean checkCanMove(String path, Player player) {
        Map map = new Map();
        map.insertFromFile(path);
        int x = player.x;
        int y = player.y;
        if (map.map.get(x).charAt(y) == ' ') {
            checkMove = true;
        } else {
            checkMove = false;
        }
        return checkMove;
    }*/
}
